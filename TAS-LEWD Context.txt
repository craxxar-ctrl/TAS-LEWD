// TAS LEWD Context Script

// Every script needs a modifier function
const modifier = (text) => {
  text = onContext_TASLEWD(text);
 
  return { text }
}

// Don't modify this part
modifier(text)
